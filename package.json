{
  "name": "btc-lottery-pet",
  "private": true,
  "version": "0.7.0",
  "type": "module",
  "scripts": {
    "dev": "vite --configLoader runner",
    "build": "tsc && vite build --configLoader runner",
    "test:frontend": "tsc -p tsconfig.frontend-test.json && node --test test/miningLogic.test.mjs",
    "preview": "vite preview --configLoader runner",
    "clean:dev:check": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/clean-dev.ps1 -WhatIf",
    "clean:dev": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/clean-dev.ps1",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^6.0.2",
    "typescript": "~5.6.2",
    "vite": "^8.0.16"
  }
}
